{
    "name": "bitstation",
    "scripts": {
	"build": "docker-compose build --no-cache bitstation",
	"build-pkgs": "docker-compose build --no-cache cauldron",
	"proxy-set": "cp proxy.sh 01.cauldron; cp proxy.sh 03.setup",
	"start": "docker-compose up bitstation",
	"stop": "docker-compose stop bitstation",
	"proxy-start": "./utils/proxy-start.sh",
	"proxy-install": "./utils/proxy-install.sh",
	"shell": "./utils/shell-server.sh bitstation"
    }
}
